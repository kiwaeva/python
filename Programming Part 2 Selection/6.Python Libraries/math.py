"""Python libraries are a set of useful functions
that eliminate the need for writing codes from scratch"""

#import math library so we can use it
import math
radius = float(input("Enter radius:"))
#invoke the math.pi metod from the math library
area = math.pi * radius ** 2

print("The answer is:", area)
